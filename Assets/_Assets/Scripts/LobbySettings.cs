using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class LobbySettings : MonoBehaviour
{

    [SerializeField] private Slider numRacers_Slider;
    [SerializeField] private InputField numRacers_InputField;

    [SerializeField] private Slider numRacesToPlay_Slider;
    [SerializeField] private InputField numRacesToPlay_InputField;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
